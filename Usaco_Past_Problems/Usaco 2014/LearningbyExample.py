read = open("mowing.in")
n = int(read.readline())
coor = [[0,0]]
curr_pos = [0,0]

def point(start, end):
    if start[0] < end[0]

for i in range(n):
    d, s = read.readline().split()
    s = int(s)
    temp = curr_pos
    if d == "N":
        curr_pos[1] += s
    elif d == "S":
        curr_pos[1] -= s
    elif d == "E":
        curr_pos[0] += s
    else:
        curr_pos[0] -= s

    for j in range(1, )

        


